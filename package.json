{
  "name": "codex-worker",
  "version": "0.1.0",
  "description": "A persistent task runner for Codex CLI — schedule, resume and automate long-running coding tasks.",
  "type": "module",
  "bin": {
    "codex-worker": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "commander": "^13.1.0",
    "execa": "^9.5.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
